spring:
  application:
    name: zone-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: false

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 8081

# Fallback for external IoT base URL if Config Server is unavailable
external:
  iot:
    base-url: http://104.211.95.241:8080/api
    token: ""
